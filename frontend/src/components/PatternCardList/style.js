import { Row } from 'react-bootstrap';

import styled from 'styled-components';

export const CardRow = styled(Row)`
  margin: 0 calc(-1 * var(--bs-gutter-x) * 0.5) 0
    calc(-1 * var(--bs-gutter-x) * 0.5);
`;
